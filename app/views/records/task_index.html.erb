<!--そのカテゴリに属するタスク一覧のページだよ。こっちはタスク記録画面へ飛ぶよ-->
<div class="container-fluid">
  <div class="center-block">
    <%= form_with model: @record, url: family_records_path(@family) do |f| %>
      <%= turbo_frame_tag @category do %>
        <%= link_to new_family_record_path(@family), class: "text-decoration-none text-body" do %>
          <div class="row bg-light py-2 mx-auto border-bottom border-top border-secondary">
            <div class="col-1">▼</div>
            <div class="col-11"><%= @category.name %></div>
          </div>
        <% end %>
        <% @category.tasks.each do |task| %>
          <%= render partial: 'task_for_record', locals: { task: task, record: @record, f: f } %>
        <% end %>
        <div class="bg-light center-block text-center mb-2 border-top">
          <%= f.submit '記録', class: 'text-center btn btn-success my-2' %>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
